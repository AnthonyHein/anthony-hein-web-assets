export interface NavigationItem {
  name: string;
  slug: string;
}

const navigationData: NavigationItem[] = [
  {
    name: "Section",
    slug: "section",
  },
  {
    name: "Contact",
    slug: "contact",
  },
];

export default navigationData;
